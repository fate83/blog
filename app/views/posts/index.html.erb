<h1>Mein Blog</h1>
<%= link_to "Neuen Post erstellen", '/posts/new' %>
<% @posts.each_with_index do |post, i| %>
  <div>
    <h3> <%= "#{i+1}) " + post.title %> </h3>
    <p> <%= post.content %> </p>
  </div>
<% end %>
